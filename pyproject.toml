[project]
name = "outlook-masive-emails"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4",
    "extract-msg",
    "pandas",
    "python-dotenv",
    "pywin32; sys_platform == 'win32'",
    "requests",
]

[dependency-groups]
test = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = [
    ".",
]
testpaths = [
    ".",
]
